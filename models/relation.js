class Relation {
    constructor(id, bossId, userId, status) {
        this.id = id;
        this.bossId = bossId;
        this.userId = userId;
        this.status = status;
    }
}

export default Relation;